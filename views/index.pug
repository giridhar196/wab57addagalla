extends layout

block content
  h1= title
   p Heroku server is used .
  button(type="button", class="btn btn-primary" onClick="method()") Press Me
  div#target
  a(href='serials') Serials


  script.
    var client = 0
    function method(){
      client++
      console.log(client)
      document.getElementById("target").innerHTML = "Value now " +client;
    } 